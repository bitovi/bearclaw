name: Publish Storybook
on:
  push:
    branches:
      - dev
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false
      - name: Install and Build 🔧
        run:
          | # Install npm packages, build the Storybook files, adds a ./storybook-static/.nojekyll file, and copies these over to /docs
          npm install
          npm run storybook:publish
